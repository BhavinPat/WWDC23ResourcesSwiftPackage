import XCTest
@testable import WWDC23ResourcesSwiftPackage

final class WWDC23ResourcesSwiftPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WWDC23ResourcesSwiftPackage().text, "Hello, World!")
    }
}
